/**
 * Diagram Wizard Hooks
 *
 * Custom React hooks for managing diagram wizard state and interactions
 */

// Export the primary diagram session hook for use in components
export { useDiagramSession } from './useDiagramSession';

// Export the type definition for configuration options of the diagram session hook
export type { UseDiagramSessionOptions } from './useDiagramSession';

The comments explain:
1. What the export of `useDiagramSession` does (provides the hook)
2. What the type export represents (configuration type for the hook)

Would you like me to elaborate on any part of the comments?